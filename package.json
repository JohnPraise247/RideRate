{
  "name": "riderate",
  "version": "1.0.0",
  "description": "Keeps you informed of the present vehicle prices for your travel needs.",
  "scripts": {
    "dev": "webpack-dev-server --config webpack.dev.js",
    "build": "fsbin emptyDir build && fsbin copy public ./ && webpack --config webpack.build.js",
    "clean": "fsbin emptyDir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aos": "^3.0.0-beta.6",
    "clean-css": "^5.3.2",
    "jquery": "^3.7.0",
    "mithril": "^2.2.2"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "fsbin": "^1.0.10",
    "webpack": "^5.51.2",
    "webpack-cli": "^4.8.0",
    "webpack-dev-server": "^4.1.0"
  }
}
